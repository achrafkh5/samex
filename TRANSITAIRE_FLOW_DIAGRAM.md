# Mandant Transitaire Module - System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         TRANSITAIRE MODULE ARCHITECTURE                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│                              ADMIN FLOW                                       │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────┐
    │ Admin Login │
    └──────┬──────┘
           │
           ▼
    ┌──────────────────┐
    │ Admin Dashboard  │
    │  - Sidebar Menu  │
    └──────┬───────────┘
           │
           │ Click "Customs Brokers"
           ▼
    ┌──────────────────────────────────┐
    │  TransitairesModule.js           │
    │  ┌────────────────────────────┐  │
    │  │  Search Bar                │  │
    │  │  [Add Customs Broker]      │  │
    │  ├────────────────────────────┤  │
    │  │  Table:                    │  │
    │  │  - Agreement #             │  │
    │  │  - Name/Company            │  │
    │  │  - NIF                     │  │
    │  │  - Wilaya                  │  │
    │  │  - [Edit] [Delete]         │  │
    │  └────────────────────────────┘  │
    └─────────┬────────────────────────┘
              │
              ├─── Add ───────┬──→ POST /api/transitaires ──┐
              │                │                             │
              ├─── Edit ──────┼──→ PUT /api/transitaires/[id] ──┐
              │                │                                 │
              └─── Delete ────┼──→ DELETE /api/transitaires/[id]│
                               │                                 │
                               ▼                                 ▼
                        ┌──────────────────┐           ┌──────────────┐
                        │ Transitaire.js   │◄──────────┤  MongoDB     │
                        │  (Model)         │           │  dreamcars   │
                        │  - create()      │           │  Collection: │
                        │  - findAll()     │           │  transitaires│
                        │  - update()      │           └──────────────┘
                        │  - delete()      │
                        └──────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT FLOW                                      │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌────────────────┐
    │ Client Access  │
    │ /inscription   │
    └────────┬───────┘
             │
             ├──→ Step 1: Personal Info
             ├──→ Step 2: Car Selection
             ├──→ Step 3: Payment
             │
             ▼
    ┌────────────────────────────────────────┐
    │ Step 4: Documents                      │
    │  ┌──────────────────────────────────┐  │
    │  │ Upload Passport                  │  │
    │  │ Upload National ID               │  │
    │  │ Upload Birth Certificate         │  │
    │  │ Upload Proof of Residence        │  │
    │  ├──────────────────────────────────┤  │
    │  │ TransitaireSelector Component    │  │
    │  │  ┌────────────────────────────┐  │  │
    │  │  │ Select Customs Broker ▼    │  │  │
    │  │  │  - ABC Customs - Alger     │  │  │
    │  │  │  - XYZ Services - Oran     │  │  │
    │  │  └────────────────────────────┘  │  │
    │  │  [Download Mandate]              │  │
    │  └──────────────────────────────────┘  │
    │  ┌──────────────────────────────────┐  │
    │  │ Upload Signed Mandate            │  │
    │  └──────────────────────────────────┘  │
    └────────┬───────────────────────────────┘
             │
             │ 1. Select Transitaire
             ▼
    GET /api/transitaires
             │
             ▼
    ┌────────────────────────┐
    │ Fetch all transitaires │
    │ Display in dropdown    │
    └────────┬───────────────┘
             │
             │ 2. Click Download
             ▼
    ┌─────────────────────────────────┐
    │ generateMandatPDF() (Client)    │
    │  - Uses jsPDF library           │
    │  - Creates PDF in browser       │
    │  - Pre-fills transitaire data   │
    │  - Leaves "Fait à" blank        │
    │  - Leaves "le" blank            │
    │  - Auto-downloads                │
    └─────────┬───────────────────────┘
              │
              │ 3. Client fills blanks & signs
              │
              ▼
    ┌──────────────────────────┐
    │ Client uploads signed    │
    │ PDF via FileUploader     │
    └──────────┬───────────────┘
               │
               │ 4. Submit Form
               ▼
    ┌──────────────────────────────┐
    │ Upload to Cloudinary         │
    │ Save URL in MongoDB          │
    │ Complete inscription         │
    └──────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                           PDF GENERATION FLOW                                 │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │ User clicks         │
    │ "Download Mandate"  │
    └──────────┬──────────┘
               │
               ▼
    ┌──────────────────────────────────┐
    │ Validate: Transitaire selected?  │
    └──────────┬───────────────────────┘
               │ Yes
               ▼
    ┌──────────────────────────────────┐
    │ Get transitaire object from      │
    │ state (already fetched via API)  │
    └──────────┬───────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────┐
    │ Create PDF (jsPDF)                           │
    │  1. Initialize A4 portrait                   │
    │  2. Add title: "MANDAT"                      │
    │  3. Add client fields (blank lines)          │
    │  4. Add transitaire section:                 │
    │     ✅ Numéro d'agrément: [DB VALUE]         │
    │     ✅ Nom/Raison sociale: [DB VALUE]        │
    │     ✅ NIF: [DB VALUE]                       │
    │     ✅ Wilaya: [DB VALUE]                    │
    │  5. Add mission text                         │
    │  6. Add signature section (blank):           │
    │     ❌ Fait à: _______ (BLANK)               │
    │     ❌ le: _______ (BLANK)                   │
    │  7. Add footer                               │
    └──────────┬───────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────┐
    │ doc.save('Mandat_[Name].pdf')    │
    │ → Downloads to user's device     │
    └──────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                           DATABASE SCHEMA                                     │
└──────────────────────────────────────────────────────────────────────────────┘

    MongoDB: dreamcars.transitaires
    
    {
      _id: ObjectId("..."),
      num_agrement: "AG-2024-001",           // Unique
      nom_ou_raison_sociale: "ABC Customs",
      nif: "123456789012345",
      wilaya: "Alger",                       // From 58 wilayas
      createdAt: ISODate("..."),
      updatedAt: ISODate("...")
    }
    
    Indexes:
    - _id (default)
    - num_agrement (unique, for duplicate prevention)


┌──────────────────────────────────────────────────────────────────────────────┐
│                           API ENDPOINTS MAP                                   │
└──────────────────────────────────────────────────────────────────────────────┘

    /api/transitaires
    ├── GET     → List all (Public)
    └── POST    → Create new (Admin only)

    /api/transitaires/[id]
    ├── GET     → Get by ID (Public)
    ├── PUT     → Update (Admin only)
    └── DELETE  → Delete (Admin only)

    /api/generate-mandat
    └── POST    → Get transitaire data for PDF (Public)


┌──────────────────────────────────────────────────────────────────────────────┐
│                       COMPONENT HIERARCHY                                     │
└──────────────────────────────────────────────────────────────────────────────┘

    AdminDashboardContent
    └── TransitairesModule
        ├── Search Bar
        ├── Add Button → Modal
        │   └── Form (num_agrement, nom, nif, wilaya)
        ├── Table
        │   └── Row → Edit Button → Modal
        │            └── Delete Button
        └── Toast Notification

    InscriptionPageContent
    └── Step 4: Documents
        ├── FileUploader (Passport)
        ├── FileUploader (National ID)
        ├── FileUploader (Birth Certificate)
        ├── FileUploader (Proof of Residence)
        ├── TransitaireSelector ← NEW
        │   ├── Dropdown (fetches /api/transitaires)
        │   └── Download Button (generates PDF)
        └── FileUploader (Signed Mandate) ← Existing


┌──────────────────────────────────────────────────────────────────────────────┐
│                       SECURITY & AUTH FLOW                                    │
└──────────────────────────────────────────────────────────────────────────────┘

    Public Endpoints (No Auth):
    ✅ GET /api/transitaires
    ✅ GET /api/transitaires/[id]
    ✅ POST /api/generate-mandat

    Protected Endpoints (Admin Auth Required):
    🔒 POST /api/transitaires
    🔒 PUT /api/transitaires/[id]
    🔒 DELETE /api/transitaires/[id]

    Auth Flow:
    Request → verifyAdmin() → Check admin_auth_token cookie
                           → Verify JWT
                           → Return { valid: true/false, admin }


┌──────────────────────────────────────────────────────────────────────────────┐
│                       TRANSLATION KEYS                                        │
└──────────────────────────────────────────────────────────────────────────────┘

    LanguageProvider.js
    ├── English (en)
    │   ├── transitairesManagement: 'Customs Brokers Management'
    │   ├── addTransitaire: 'Add Customs Broker'
    │   ├── agreementNumber: 'Agreement Number'
    │   └── ... (20+ keys)
    ├── French (fr)
    │   ├── transitairesManagement: 'Gestion des Transitaires'
    │   ├── addTransitaire: 'Ajouter un Transitaire'
    │   └── ...
    └── Arabic (ar)
        ├── transitairesManagement: 'إدارة وكلاء الجمارك'
        ├── addTransitaire: 'إضافة وكيل جمارك'
        └── ...
```
